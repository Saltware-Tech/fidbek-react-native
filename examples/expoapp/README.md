# Expo Example (Source-linked)

This app uses the local package source instead of npm release:

- `@saltware/fidbek-react-native`: `file:../..`

## Run

```bash
cd /Users/talha/Desktop/Fidbek/fidbek-react-native/examples/expoapp
npm install
npm run ios
```

Any change under `/Users/talha/Desktop/Fidbek/fidbek-react-native` is picked up after reinstall/rebuild.
